'use strict';

describe('Avengers.characters module', function() {

  beforeEach(module('Avengers.characters'));

  describe('characters controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var charactersCtrl = $controller('charactersCtrl');
      expect(charactersCtrl).toBeDefined();
    }));

  });
});